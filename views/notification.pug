doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    title Done And Done!
    style.
      body {
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        height: 100vh;
        margin: 0;
        font-family: Arial, sans-serif;
        background-color: #edf8f9;
        position: relative;
      }
      .container {
        text-align: center;
      }
      .logo {
        width: 100px; 
        position: absolute;
        top: 20px; 
        left: 20px; 
      }
     .notification {
        padding: 20px 30px;
        border-radius: 5px;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        background-image: -webkit-linear-gradient(left top, #067c89 0%, #0e868f 30%, #1d9b9b 55%, #29aca3 100%);
        color: #fff; 
        font-size: 18px;
        margin-top: 20px;
      }
      .notification .success-title {
        font-weight: bold;
        margin-bottom: 10px;
      }
      .button {
        display: inline-block;
        margin-top: 20px;
        padding: 10px 20px;
        font-size: 16px;
        color: #fff;
        background-color: #007bff;
        border: none;
        border-radius: 5px;
        text-decoration: none;
      }
      .info {
        margin-top: 10px;
        color: #6c757d;
      }
      .info {
        margin-top: 10px;
        color: #008d9b;
      }
      .button-return {
        padding: 10px 20px; 
        border-radius: 15px; 
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        background-image: -webkit-linear-gradient(left top, #067c89 0%, #0e868f 30%, #1d9b9b 55%, #29aca3 100%);
        color: #fff;
        text-decoration: none;
      }
  body
    img.logo(src='/img/footer_logo.png' alt="Logo")
    .container
      h1 Done And Done!
      .notification
        .success-title Success!
        p We've sent an email to #{userEmail} with password reset instructions.
      .info
        p If the email doesn't show up soon, check your spam folder. We sent it from support@mailsac.com.  
      a.button.button-return(href="/login") Return To Login
